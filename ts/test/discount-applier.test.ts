describe('discount applier tests', () => {
  it('apply v1', () => {
    // TODO: write a test that fails due to the bug in
    // DiscountApplier.applyV1
    expect(1).toEqual(1);
  });

  it('apply v2', () => {
    // TODO: write a test that fails due to the bug in
    // DiscountApplier.applyV2
    expect(1).toEqual(1);
  });
});
